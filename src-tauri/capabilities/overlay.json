{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Capabilities for the quick-query overlay window (⌘⇧Space).",
  "windows": ["overlay"],
  "permissions": [
    "core:default"
  ]
}

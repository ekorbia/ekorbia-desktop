{
  "name": "ekorbia-ui-tests",
  "private": true,
  "description": "Test-runner tooling only. The production UI is no-bundler and never reads this file. See CLAUDE.md for the architecture rule.",
  "scripts": {
    "test:ui": "playwright test",
    "test:ui:headed": "playwright test --headed",
    "test:ui:debug": "playwright test --debug",
    "test:ui:trace": "playwright show-trace"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
